# API base URL. In development, Vite proxies /api to json-server (localhost:3001).
# Leave empty to use /api (proxy). For production, set to your deployed API URL.
# Example: VITE_API_URL=https://your-api.example.com
VITE_API_URL=https://user-backend.onrender.com/api
